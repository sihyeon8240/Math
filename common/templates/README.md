# __BOOK_TITLE__

Publication status and automation flags are owned by the `books.yml` entry.

Build from the repository root:

    make book BOOK=__BOOK_SLUG__

Before adding labels, assign a short course prefix and replace the `xx` prefix
in the starter chapter and section. See `docs/writing-guide.md`.
